# P0-24 验收记录

- 任务：统一指标口径说明
- 实现日期：2026-07-26

## 已完成

- 每个指标能力现在都带有来源表、数据粒度、计算公式和去重键。
- 订单 GMV 明确来自订单主表 order_amount，并按 order_id 去重。
- 行为 GMV 明确只统计 behavior=buy，并按 price × amount 计算，不做订单去重。
- 退款率明确采用金额退款率：退款金额 ÷ 订单 GMV × 100%。
- 退款记录按退款金额汇总，订单 GMV 按 order_id 去重，避免退款表连接订单明细后重复累计。
- 购买用户数、商品排行、渠道贡献、复购率和行为漏斗均显示各自粒度与去重键。
- 字段确认页的指标能力清单直接展开显示中文口径说明。

## 专项验收

- 每个指标均存在来源、粒度、公式和去重键。
- 订单 GMV 公式包含 order_amount 和 order_id 去重口径。
- 退款率公式包含订单 GMV 分母。
- npm test：通过，包含指标口径完整性测试。
- npx tsc -b --pretty false：通过。
- npm run build：待最终运行。
