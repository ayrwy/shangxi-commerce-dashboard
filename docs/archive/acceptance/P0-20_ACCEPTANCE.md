# P0-20 验收记录

- 任务：增加“每行代表什么”确认
- 实现日期：2026-07-26

## 已完成

- 在文件角色前新增“每行代表”选择：一次行为、一个订单、一个订单商品、一个商品、一个用户、一次退款、自定义。
- 根据文件角色和表头自动推荐粒度；订单表同时出现 product_id、goods_id 或 sku 时推荐“一个订单商品”。
- 每个选项显示中文示例说明，例如订单明细允许同一 order_id 出现多行。
- 粒度可人工修改，修改角色时自动重新推荐，修改粒度后自动取消已确认状态。
- 角色与粒度冲突时显示具体文件、角色、粒度和修复建议，并阻止“确认字段映射”。
- 旧 sessionStorage 会话缺少粒度字段时自动补齐推荐值，避免恢复旧会话直接崩溃。

## 专项验收

- 行为表：推荐“一次行为”。
- 订单主表：推荐“一个订单”。
- 订单宽表：检测订单字段与商品字段同时存在，推荐“一个订单商品”，并提示当前角色冲突，要求用户修改角色或粒度。
- 未知表：推荐“自定义”，允许继续人工确认。
- npm test：通过，包含粒度推荐与冲突阻断测试。
- npx tsc -b --pretty false：通过。
- npm run build：待最终运行。
