# P0-21 验收记录

- 任务：阻止同一文件重复映射标准字段
- 实现日期：2026-07-26

## 已完成

- 同一文件内，同一个标准字段默认只能由一列使用。
- 字段下拉会禁用已经被同文件其他列使用的标准字段，并显示“已被其他列使用”。
- 对旧会话、手动修改或外部恢复造成的重复映射，页面仍会再次检测并阻止确认。
- 冲突区显示文件名、重复的源列名和对应标准字段。
- 提供“取消后一列映射”修复入口，将后一列恢复为“不映射”。
- 未引入多列合并语义；如需合并，后续进入独立字段转换任务。

## 专项验收

- goods_id 和 sku_id 同时映射为 product_id 时，检测结果包含两列名称。
- 重复映射存在时，“确认字段映射”按钮不可用。
- 取消后一列映射后，冲突消失，用户可以继续确认。
- npm test：通过，包含重复标准字段检测测试。
- npx tsc -b --pretty false：通过。
- npm run build：待最终运行。
